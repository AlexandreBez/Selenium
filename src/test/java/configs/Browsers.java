package configs;

/**
 * Enumeration for browsers options to select in DriverFactory class
 * @author Lucas Alexandre Bez Piancoski
 * @since 1.0
 */
public enum Browsers {
	CHROME,
	FIREFOX,
	EDGE
}
